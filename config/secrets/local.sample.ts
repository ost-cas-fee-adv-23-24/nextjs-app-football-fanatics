// Private file to local development
// This file should be used to store secret configurations.
// rename it to secrets.ts (It must not be added to the repo)

const local = {
  zitadelClientIdentifier: 'XXXXXXXXXXXXXXXXXXX',
};
export default local;
